{
  "name": "@modelcontextprotocol/server-ftp",
  "version": "0.1.0",
  "description": "MCP server wrapper for FTP access (used to bridge gcloud-ftp)",
  "license": "MIT",
  "author": "web3tony",
  "type": "module",
  "bin": {
    "mcp-server-ftp": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.25.2",
    "basic-ftp": "^5.0.4",
    "zod": "^3.23.8",
    "zod-to-json-schema": "^3.23.5"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.14",
    "@types/node": "^22",
    "jest": "^29.7.0",
    "shx": "^0.3.4",
    "ts-jest": "^29.4.6",
    "typescript": "^5.6.2"
  }
}
